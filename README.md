# Campus Notifications

## Run

```bash
cd notification_app_fe
npm install 
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## API setup

1. **Settings** → Register (6-character `accessCode`)
2. Save `clientID` / `clientSecret` → **Generate Token**



API: `http://4.224.186.213/evaluation-service`

